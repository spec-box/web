import { cn } from '@bem-react/classname';

export const RATE_LIMIT = 20;
export const COLORS: Record<string, string> = {
  success: 'green',
  error: 'red',
  warn: 'orange',
};
export const INDENT = 32;

export const bem = cn('ProjectFeatures');
